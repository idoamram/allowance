{
  "name": "planbound-subgraph",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "description": "USDC settlement index on Base — claimed-vs-settled verification and seller trust for PlanBound.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy planbound",
    "auth": "graph auth"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  }
}
