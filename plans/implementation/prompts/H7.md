# Session prompt — Task H7: loading and transition experience

Committed per the event rule. Issued by the control tower 2026-07-26 ~01:10, at Ido's
request: *"we should make the loading time experience much better with creative polished
animation."*

**Why it matters here specifically:** approving a plan mints a Hedera account, schedules its
refund, and writes two HCS messages. That is ~10–20 seconds of real consensus work behind
one button press. Right now the button just sits disabled. On the demo video that reads as
a hang; it should read as the product doing something consequential.

```
You are a PlanBound implementation session. Read AGENTS.md and
plans/product-spec/latest.md first — they govern everything.
Your task: H7 — the loading, pending and transition experience. UI ONLY.

Contract files are FROZEN: packages/core/*, supabase/migrations/*. Do NOT touch any
API route, packages/chains/**, packages/mcp/**, scripts/**, or subgraph/**. No
behaviour changes, no new network calls, no changes to what any action does.
You own: apps/web/app/p/[id]/** (presentation only), apps/web/app/console/**,
apps/web/app/globals.css, and any new component files you add.

Branch: task/h7-loading-polish. Commit continuously; PR when green; merge it
yourself. Port 3007. Never read .env.local; verify vars with `test -n` only.

The moments that need work, in priority order:

1. **Approve → envelope minted (~10–20s).** The single most important one; it is the
   product's climax and it is currently a disabled button. Show that something real
   is happening on a public ledger: the envelope being created, the refund being
   scheduled, the receipt being written. Do not fake progress you cannot observe —
   if you show steps, they must be honest about being indicative rather than
   measured, or be driven by something real.
2. **Step-up verification** — between tapping "Verify with World ID" and the widget
   resolving.
3. **Drift decision** (approve / re-plan / abort) — each triggers chain work.
4. **Page transitions** between plan states (pending → approved → executing →
   blocked → settled). Today the page just re-renders.

Design constraints:
- The identity is manila/ink; tokens already exist in globals.css. This should feel
  like a considered financial document that is working, not like a SaaS spinner.
- Respect `prefers-reduced-motion` — provide a still, legible fallback.
- No external animation libraries; a strict CSP applies in production. CSS and
  small inline SVG only.
- Mobile first. The approval page is opened on a phone.
- Keep it fast: no layout shift, no blocking the paint.

Verify with the Playwright MCP browser at several viewport sizes, and screenshot the
states into docs/feedback/img/ so the PR shows what changed.

When done: reply with what shipped, file:line pointers, and anything you left
honest-but-unfinished.
```
