# Session prompt — Task H6: Substreams skill (Worldchain settlements)

Committed per the event rule. Issued by the control tower 2026-07-26 ~00:00, at Ido's
suggestion, after Subgraph Studio dropped subgraph support for Worldchain (see H3 / PR #31
and `.feedbacks/thegraph.md`).

**Why this exists:** the subgraph now indexes Base. Worldchain settlements are therefore
unindexed, which is the only reason we demoted Worldchain as a settlement rail. A working
Substreams index of Worldchain USDC removes that constraint — we settle on both rails and
have chain-verified data for both. Packaged as a Claude Code skill because that is what this
product already is: an agent that does a real job end to end.

```
You are a PlanBound implementation session. Read AGENTS.md and
plans/product-spec/latest.md first — they govern everything.
Your task: Task H6 ONLY.

HARD TIMEBOX: 90 minutes from start. This is a stretch task on a submission
deadline. At 90 minutes, stop wherever you are, commit what is real, and report.
Do not push past it — the core demo is someone else's critical path and must not
be destabilised for this.

Contract files are FROZEN: packages/core/*, supabase/migrations/*. Do NOT touch
subgraph/** (the Base subgraph works and is not yours), apps/web/app/p/**,
apps/web/app/api/** except the one new route named below, packages/mcp,
scripts/driver.ts, or packages/chains/x402pay.ts.
You own: plugin/skills/index-settlements/**, substreams/**, and (only if the index
actually runs) apps/web/app/api/mcp/worldchain-trust/route.ts.

Branch: task/h6-substreams-skill. Commit continuously; PR when green; merge it
yourself. Port 3006. Never read .env.local; verify vars with `test -n` only.

STOP CONDITIONS — report and stop, do not improvise around them:
- No prebuilt ERC-20 .spkg covers Worldchain, AND writing Rust would be needed.
- Consuming the stream needs infrastructure we cannot run inside this repo
  (a long-lived sink process, a database we do not have, paid-tier access).
- Deploying needs credentials Ido has not provided.
In any of those cases the honest outcome is "here is exactly what blocks it",
which is a real result. Do not ship a skill that scaffolds something unrunnable.

WHAT TO BUILD, in this order — each step must work before the next starts:

1. VERIFY FIRST, BUILD SECOND. Establish, against real docs and real commands:
   - Does Substreams support Worldchain (network id? endpoint?)
   - Is there a prebuilt ERC-20 transfers .spkg usable without writing Rust?
     Candidates: streamingfast/substreams-erc20-balance-changes,
     pinax-network/substreams-erc20, spkg.io registry.
   - What does consuming it actually require — `substreams run`/`gui`, a sink,
     an auth token, a paid endpoint?
   Write findings to .feedbacks/thegraph.md AS YOU GO (that file is gitignored and
   is our sponsor-facing feedback; append, do not rewrite what is there).
   If this step says "not feasible tonight", that IS the deliverable. Stop there.

2. Only if feasible: get ONE real query returning real Worldchain USDC transfers.
   Prove it with actual output in your report. Nothing else counts as working.

3. Only then: the skill. plugin/skills/index-settlements/SKILL.md — a skill that
   takes a chain + token address, produces a runnable Substreams package, verifies
   it returns data, and says plainly what it cannot do. The skill must be honest
   about its own limits; a skill that pretends to deploy is worse than none.

4. Only if 2 and 3 are real: expose the data to the console the same way H3's
   seller-trust does, via the one route named above. Do not modify H3's panel.

Everything these SDKs tell you is newer than your training data. Read real docs,
run real commands, and say "I don't know" rather than inventing an API surface.

When done: reply with what shipped, what you VERIFIED versus what you assumed,
file:line pointers, and anything you left honest-but-unfinished.
```
