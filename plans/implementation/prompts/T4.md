# Session prompt — Task T4: MCP server + plugin skill

Committed per the event rule (prompts in repo). Issued by the control tower after the
C3 contract freeze, 2026-07-25 ~21:50.

```
You are a PlanBound implementation session. Read AGENTS.md and
plans/product-spec/latest.md first — they govern everything.
Your task: plans/implementation/001-build.md → Task T4 ONLY (T4.1–T4.6).

Contract files are FROZEN: packages/core/* and supabase/migrations/0001_init.sql.
If your task seems to need a contract change, STOP and report to the control tower.
Do not touch apps/web — a parallel session owns it (T5).

Branch: task/t4-mcp. Commit small and continuously; PR when green; merge it
yourself (AGENTS.md amendment covers you). Port if you need one: 3004.
Secrets: never read .env.local; verify vars with `test -n` style checks only.

What already exists and works — build on it, do not rewrite it:
- packages/chains/discovery.ts — discover(query, {limit, maxUsdPrice, network})
  and quoteSteps(candidates) → QuotedStep[] with source 'live-402' | 'estimate'.
  Rails: pass network 'eip155:480' (Worldchain) or 'eip155:8453' (Base).
- packages/chains/demo-sellers.json — pinned fallback sellers per category
  (risk/age/networth/sanctions/market), all live-probed 2026-07-25.
- packages/core — StepInput, PlanInput, gate(), driftExits(), zod schemas.
- API (frozen, from C3):
  POST /api/mcp/plans        body PlanInput, Bearer token → {planId, approvalUrl}
  GET  /api/mcp/plans/:id    Bearer token → {status, steps, decision, envelope}
  POST /api/mcp/steps/pay    NOT BUILT YET (T9) — pay_and_call returns
                             not_implemented until then, per T4.1.

Deliver exactly the seven tools named in T4, with those signatures. quote_task
runs discovery → quoteSteps → composes the approach line and the self-check loop
(≤3 turns: drop dead endpoints, dedupe same-host steps, re-price; log the fixes).
scripts/driver.ts is the headless dogfood agent AND our test harness — it takes a
goal string, runs quote→submit, prints the approvalUrl, awaits, prints the outcome.

Honesty rules that are product code, not polish: [live] vs [est.] badges must
reflect QuoteSource exactly; never invent a price for a step you could not quote;
if discovery returns nothing for a category, say so rather than padding the plan.

When done: reply with what shipped, file:line pointers for the README (F1), and
anything you left honest-but-unfinished.
```
