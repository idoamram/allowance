# Session prompt — Task T5: approval page + console list

Committed per the event rule (prompts in repo). Issued by the control tower after the
C3 contract freeze, 2026-07-25 ~21:50.

```
You are a PlanBound implementation session. Read AGENTS.md and
plans/product-spec/latest.md first — they govern everything.
Your task: plans/implementation/001-build.md → Task T5 ONLY (T5.1–T5.3).

Contract files are FROZEN: packages/core/* and supabase/migrations/0001_init.sql.
If your task seems to need a contract change, STOP and report to the control tower.
Do not touch packages/mcp or scripts/driver.ts — a parallel session owns them (T4).
You own apps/web/app/p/** and apps/web/app/console/**. Do not modify the existing
API routes under apps/web/app/api/** (frozen contract) — read from the DB directly
in server components instead, using apps/web/lib/db.ts.

Branch: task/t5-approval-page. Commit small and continuously; PR when green;
merge it yourself. Port: 3005 (`pnpm --filter @planbound/web dev -- -p 3005`).
Secrets: never read .env.local; verify vars with `test -n` style checks only.

The page is the product. Everything else is plumbing that leads here.

/p/[id]?k=<approval_key> renders, per latest.md §5 and the mockup in
plans/product-spec/latest.html (match its copy and its manila/ink identity;
tokens are already in apps/web/app/globals.css):
  - the goal, the agent, the plan's logic line (plans.approach), and the
    self-check stamp (plans.self_check → "checked N turns: <fixes>")
  - an itemised table: service name, the step's `why` as a sub-line, the quote,
    and a [live] or [est.] badge driven by steps.source — never cosmetic
  - total vs ceiling, and an expiry countdown
  - Approve, and Reject-with-reason where the reason is TYPED
    (price | logic | scope | service) plus free text. The typed target is
    mandatory on rejection — the API enforces it and so must the form; it is the
    learning loop's only input.
Server components + server actions. The approval key stays server-side: never put
it in a client component prop, a redirect URL, or a log line. Answers POST to
/api/plans/[id]/decision?k=… (frozen route).

Also a minimal /console: plans list with status, total, ceiling, created time.
It is the operator's view; T11 extends it with receipts, so keep it simple.

Verify with the Playwright MCP browser (T5.2): open the page, approve, confirm the
plan status flips; second run, reject a step with a typed reason and confirm the
decision row exists.

Blocked on Supabase credentials in .env.local? Say so immediately and build against
a local fixture in the meantime — do not stall silently.

When done: reply with what shipped, file:line pointers for the README (F1), and
anything you left honest-but-unfinished.
```
