# Session prompt — Task H3: The Graph subgraph (Worldchain)

Committed per the event rule (prompts in repo). Issued by the control tower 2026-07-25
~22:15, ahead of the plan's "spawn after T6" note: nothing in the subgraph depends on
T6, and starting it now converts a 4am scramble into a background task.

```
You are a PlanBound implementation session. Read AGENTS.md and
plans/product-spec/latest.md first — they govern everything.
Your task: plans/implementation/001-build.md → Task H3 ONLY.

Contract files are FROZEN: packages/core/* and supabase/migrations/*. If your task
seems to need a contract change, STOP and report to the control tower.
You own subgraph/**. In apps/web you may ONLY add:
  app/api/mcp/seller-trust/route.ts and app/console/(anything you add for the
  claimed-vs-settled panel).
Do not modify existing API routes, app/p/**, packages/mcp, or scripts/driver.ts —
other sessions own those.

Branch: task/h3-subgraph. Commit small and continuously; PR when green; merge it
yourself. Port if needed: 3003. Never read .env.local; verify vars with `test -n`.

DECISION ALREADY MADE (001-build.md, H3): vanilla subgraph, NOT Substreams.
Substreams is a gated stretch only if the core is green early — do not start there.

Network: worldchain (The Graph supports it natively — verify the exact network
identifier in the Studio docs before writing the manifest; do not guess).
USDC on Worldchain: 0x79A02482A880bCE3F13e09Da970dC34db4CD24d1 (verified 2026-07-25).
Index Transfer events with a recent startBlock so it syncs in minutes — the honest
claim in the UI is "settled since deployment", never "this month".

Two uses, both load-bearing (a subgraph that mirrors our Postgres is decoration):
1. Claimed vs settled — our receipts LEFT JOIN subgraph settlements, mismatches
   highlighted. The approver verifies our backend against the chain, independent of us.
2. Seller trust — settlement counts per payTo address, exposed via
   GET /api/mcp/seller-trust?host= as a discovery-ranking input.

Plan-wallet addresses are not known at manifest time. Filter in the handler against
a registry: read seller payTo addresses from packages/chains/demo-sellers.json, and
expose plan-wallet addresses from the API for the mapping to filter on, or index
broadly and filter at query time — your call, document the tradeoff.

BLOCKED ON: the Studio deploy key (Ido). Build, codegen and validate locally; if you
cannot deploy, say so clearly rather than stalling, and leave the deploy as a single
documented command.

When done: reply with what shipped, file:line pointers for the README (F1), and
anything you left honest-but-unfinished.
```
