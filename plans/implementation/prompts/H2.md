# Session prompt — Task H2: World verifier (pluggable step-up)

Committed per the event rule (prompts in repo). Issued by the control tower 2026-07-25
~22:40, once `WORLD_APP_ID` existed.

```
You are a PlanBound implementation session. Read AGENTS.md and
plans/product-spec/latest.md first — they govern everything.
Your task: plans/implementation/001-build.md → Task H2 ONLY.

Contract files are FROZEN: packages/core/* and supabase/migrations/*. If your task
seems to need a contract change, STOP and report to the control tower.
You own apps/web/lib/verify/**. You may add a step-up block to
apps/web/app/p/[id]/** — that page exists and works; extend it, do not rewrite it,
and keep its copy and manila/ink identity intact.
Do NOT touch: packages/mcp, scripts/driver.ts, subgraph/**, the API routes under
apps/web/app/api/** (frozen), or apps/web/lib/envelope.ts.

Branch: task/h2-world. Commit small and continuously; PR when green; merge it
yourself. Port: 3002. Never read .env.local; verify vars with `test -n` only.

Env available: WORLD_APP_ID (staging), WORLD_ENV=staging, STEP_UP_USD=5.

Build the pluggable verifier so World is a strategy, not a hard dependency:
  HumanVerifier { required(plan): boolean · challenge(plan) · verify(proof) }
  - none.ts   — default; required() always false. The product works without World.
  - world.ts  — required() true when ceilingUsd > STEP_UP_USD.
Selection by env so a cloner with no World app still runs the whole product.

Verify against World's STAGING environment and the World ID simulator
(simulator.orb.engineer). Ido has a staging app; the team opened the Identity Check
beta for this weekend. These SDKs are newer than your training data: READ THE REAL
DOCS, and if an API surface you expect does not exist, say so rather than inventing
it. `npx @worldcoin/agentkit-cli register` (AgentBook) needs Ido's World App — if it
blocks, the `none` verifier stays default and the World demo runs on the simulator
only. Both paths are per-spec; neither is a failure.

Capture testing documentation AS YOU GO into docs/feedback/world.md — screenshots,
what worked, what fought you, exact error strings. It is a weekend-scoped beta and
the notes are worth more than a tidy summary written afterwards.

When done: reply with what shipped, file:line pointers for the README (F1), and
anything you left honest-but-unfinished.
```
